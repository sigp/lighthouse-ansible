---
- name: Start a testnet
  roles:
    - common
    - geth
    - lighthouse
  hosts:
    - beacon_node
    - boot_node
  vars_files:
    - ./secret.yml
  gather_facts:
    false
  tasks:
    - name: Deploy local testnet dir to host
      include_tasks: roles/lighthouse/tasks/testnet-dir-deploy.yml
